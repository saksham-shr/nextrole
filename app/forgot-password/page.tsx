import { ForgotPasswordPage } from "@/components/nextrole/auth-pages";

export const metadata = { title: "Forgot password — Braevity" };

export default function Page() {
  return <ForgotPasswordPage />;
}
