import { ResetPasswordPage } from "@/components/nextrole/auth-pages";

export const metadata = { title: "Reset password — NextRole" };

export default function Page() {
  return <ResetPasswordPage />;
}
